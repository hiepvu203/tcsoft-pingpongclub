@model IEnumerable<tcsoft_pingpongclub.Models.NhaTaiTro>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NameSponer)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UrlLogo)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NameSponer)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UrlLogo)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdSponor">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdSponor">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdSponor">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
